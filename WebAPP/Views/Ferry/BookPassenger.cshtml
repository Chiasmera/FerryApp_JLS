@using Data_Transfer_Objects.Model;
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model Passenger

<h2>Book existing by id</h2>
@using (Html.BeginForm("BookPassenger", "Ferry"))
{
    @Html.LabelFor(p => p.Id)
    @Html.EditorFor(p => p.Id)     
}
<h1>Or</h1>
<h2>Book New</h2>
@using (Html.BeginForm("BookPassenger", "Ferry"))
{
    <div>
        @Html.LabelFor(p => p.Name)
        @Html.EditorFor(p => p.Gender)
    </div>
    <div>
        @Html.LabelFor(p => p.Gender)
        @Html.EditorFor(p => p.Gender)
    </div>
}