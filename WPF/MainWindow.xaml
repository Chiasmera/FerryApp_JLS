<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ListView x:Name="ferryListView" Loaded="ferryListView_Loaded" HorizontalAlignment="Left" VerticalAlignment="Top">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Header="Name" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Cars.Count}" Header="Cars" Width="60"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=TotalPassengers}" Header="Passengers" Width="60"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=TotalIncome}" Header="Income" Width="60"/>
                </GridView>
            </ListView.View>

        </ListView>
        <TextBox x:Name="textBox" TextWrapping="Wrap" Text="TextBox" Width="120"/>

    </Grid>
</Window>
